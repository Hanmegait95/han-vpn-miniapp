# Бот Han VPN — прототип

Long polling, без зависимостей, без вебхуков. Запускается одной командой
на любой машине с Python 3:

    python3 -u bot/bot.py

Токен бота — в `~/.config/hanvpn/bot_token` (см. `bot-setup.py` в корне).
Доступы к Remnawave — в `~/.config/hanvpn/remnawave.json`; без них бот
работает на локальной памяти (`store.py`) и полностью проходим.

**Где не может жить:** на Timeweb-шаред. С их серверов `api.telegram.org`
не открывается вовсе — соединение отваливается по таймауту. Нужна VPS
за пределами блокировок или бесплатный тир вроде Fly.io.

| Файл | Что делает |
|------|------------|
| `bot.py` | Механика: опрос Telegram, отрисовка экранов, действия, напоминания |
| `screens.py` | Содержимое: тексты, кнопки, переходы. Правки — сюда |
| `remnawave.py` | Тонкий клиент панели, повторяет логику вашего бэкенда |
| `store.py` | Память: подписка, чат, живая карточка, отметки о напоминаниях |

Баннеры в `assets/` — JPEG 1280×641, по 70–100 КБ. Telegram всё равно
пережимает картинки в JPEG, поэтому исходные PNG по 0.5–2 МБ он ужимал сам,
только медленнее и с потерей на своей стороне. Пережать заново после правки
макетов: `sips -s format jpeg -s formatOptions 90 -Z 1280 исходник.png --out
assets/имя-banner.jpg`. **Имя файла сменилось — очистите `~/.config/hanvpn/file_ids.json`**,
иначе бот продолжит слать старую картинку по запомненному `file_id`.

## Жизненный цикл подписки

Корневой экран и баннер выбираются по состоянию, а не по кнопке.
`load_profile()` в `bot.py` считает `kind` и `phase`, `home()`
в `screens.py` превращает их в экран:

| kind · phase | Экран | Баннер | Главная кнопка |
|---|---|---|---|
| new | `welcome` | БЕЗЛИМИТ НА ВСЁ | 🎁 Включить 3 дня бесплатно |
| trial · active | `cabinet` | ЛИЧНЫЙ КАБИНЕТ | 🔌 Подключить VPN / 💳 Купить |
| trial · expiring (< суток) | `expiring` | ОСТАЛОСЬ 10 ЧАСОВ | 💳 Купить подписку |
| trial · expired | `expired` | VPN ОТКЛЮЧЕН | 💳 Купить подписку |
| paid · active | `cabinet` | ЛИЧНЫЙ КАБИНЕТ | 🔌 Подключить / 🛒 Продлить |
| paid · expiring | `expiring` | ОСТАЛОСЬ 10 ЧАСОВ | 🛒 Продлить подписку |
| paid · expired | `expired` | VPN ОТКЛЮЧЕН | 🛒 Продлить подписку |

После активации бесплатных дней и после оплаты — промежуточный экран
`activated`: «включено до …, остался один шаг» с единственной кнопкой
«Подключить VPN». Момент активации — пик намерения, кабинет с пятью
кнопками ему не подходит.

**Бесплатные дни включаются при первом подключении.** Отдельной кнопки нет:
мини-аппа включает их сама, когда человек нажимает «Подключить», и сообщает
боту. Второй раз не выдаются: и `act_trial()`, и обработчик `web_app_data`
проверяют `kind == 'new'`.

**Оплата — демо.** Тарифы → экран тарифа с датой «будет работать до …» →
кнопка «Оплатить (демо)» → `act_pay()` записывает `paid_until`.
Продление прибавляется к остатку, а не съедает его: купил 3 месяца
на второй день триала — получил 3 месяца плюс оставшийся день.
В бою здесь `sendInvoice` → `pre_checkout_query` → `successful_payment`,
и только после последнего — то, что делает `act_pay()`.

**Источники правды.** В прототипе всё считает `store.py`. В бою
`store` заменяет ваша база платежей (кто что купил, триал или оплата),
а Remnawave остаётся источником правды про доступ: срок (`expireAt`)
и факт подключения (`subLastOpenedAt`). `load_profile()` уже сливает
оба источника, когда панель настроена.

## Пройти весь цикл за минуту

Команда `/demo` переводит вашу запись в нужное состояние — чтобы
не ждать три дня. **В продакшене удалить.**

    /demo new         с чистого листа
    /demo trial       бесплатные дни включены, 3 дня
    /demo expiring    осталось 9 часов
    /demo expired     закончилось вчера
    /demo paid        оплачено на 3 месяца
    /demo connected   отметить «устройство настроено»
    /demo reset       всё стереть, включая отметки о напоминаниях

Сценарий проверки: `new` → «Включить 3 дня» → `activated` → «Подключить» →
`/demo connected` → кабинет 🟢 → `/demo expiring` → «Купить» → тариф →
«Оплатить (демо)» → `activated` → кабинет с датой на три месяца вперёд.

## Кнопок ровно столько, сколько нужно

Правило: **на карточке одно главное действие сверху и не больше двух
вспомогательных под ним**. Что не помещается — уходит внутрь «Помощи»
или в мини-аппу. Что из этого следует:

- «🆘 Помощь» не дублируется на карточках: она постоянно висит на нижней
  клавиатуре. Исключение — кабинет, куда человек возвращается чаще всего;
- «📘 Настройка» убрана из кабинета: это та же мини-аппа, что и
  «Подключить», — она сама спрашивает устройство. Вход остался в «Помощи»;
- «💳 Цены» убраны из «Помощи»: кнопка покупки есть на каждом корневом экране;
- «👥 Мои приглашения» убраны: те же два числа уже написаны в тексте выше;
- экраны «Канал» и «Документы» свёрнуты: экран ради одной ссылки — лишнее
  нажатие. Новости открываются кнопкой сразу, документы — ссылкой в тексте;
- кнопка со старой карточки, ведущая в удалённый экран, молча уводит домой,
  а не показывает «экран не найден».

Кабинет был из пяти кнопок, стал из трёх (у оплаченной подписки — четыре).

**Цвета у кнопок Telegram нет.** Подписи инлайн-кнопок — простой текст,
покрасить их нельзя, это ограничение клиента. Заметность делается тремя
вещами, и все три в коде: главное действие стоит первым и занимает всю
ширину, вспомогательные — парой в строке ниже; значок главного действия
цветной и говорит о деньгах или подключении (💳 🛒 🔌); фон задаёт баннер —
у экрана «VPN отключён» он красный, у кабинета зелёный. Настоящий цвет
кнопки есть только в мини-аппе: `MainButton` там зелёная, а в тревожном
состоянии красная (`index.html`, `setParams`).

## Анимация: три вида, все доступны обычному боту

Проверено на этом боте, без Premium и без покупок на Fragment:

| Что | Как | Где включено |
|---|---|---|
| Анимированные эмодзи в тексте | `<tg-emoji emoji-id="…">🎁</tg-emoji>` в HTML-подписи | `S.animate()` — прогоняет любую подпись перед отправкой |
| Эффект на весь экран | `message_effect_id` в `sendPhoto`/`sendMessage` | включили дни, оплатили, подключились |
| Реакция бота | `setMessageReaction` | 👀 на любое письмо человека — видно, что прочли |

Внутри `<tg-emoji>` остаётся обычный значок: клиент, который не покажет
анимацию, покажет его — текст не сломается ни при каких условиях.
`ANIMATED = False` в `screens.py` выключает всё разом.

**В кнопках анимации не бывает.** Подписи кнопок — простой текст, и это
не обойти. Поэтому анимированные значки стоят в последней строке подписи,
прямо над кнопкой, куда надо нажать.

**Эффект показывается только на новом сообщении.** При правке старого
(`editMessageMedia`) его не видно, поэтому в пиковые моменты бот не правит
карточку, а пересылает её: старая удаляется, новая приходит с салютом.
Пиковых моментов ровно три — иначе эффект перестаёт радовать.

Идентификаторы эмодзи взяты из открытых наборов Telegram через
`getStickerSet` (`Animated Emoji`, `Hand Emoji` и другие) — обновить можно
тем же способом. Идентификаторы эффектов — в константах `EFFECT_*`.

## Помощь в одно нажатие

Три инструмента Bot API, которые снимают вопрос «куда нажать, если не вышло»:

**Постоянная клавиатура** под полем ввода — «🔌 Подключить VPN», «🏠 Кабинет»,
«🆘 Помощь». Не зависит от того, какая карточка на экране: помощь всегда
в одном нажатии. Отправляется один раз на версию (`KEYBOARD_VERSION`),
чтобы не спамить. «Подключить VPN» здесь — KeyboardButton с web_app: только
так мини-аппа может ответить боту (`sendData` из инлайн-кнопок не работает).

**Поддержка — отдельный бот.** В этого бота она не зашита: на экране
«Помощь» кнопка «Написать нам» ведёт по `SUPPORT_URL` (`screens.py`,
пока заглушка). Любой текст, который не команда, бот всё равно не оставляет
без ответа: по приметам показывает нужный раздел — «сколько стоит» →
тарифы, «настроить» → инструкции, «не работает» → помощь, остальное → домой.

**Мини-аппа — часть потока, не отдельная страница.** Демо-панели в ней нет:
имя из Telegram, состояние из адреса, который подставляет бот
(`?kind=new|trial|paid&until=…`), и из памяти браузера. Бот узнаёт о том,
что случилось внутри, из `sendData` (`trial`, `paid`, `connected`, `failed`).

**Минимум нажатий.** Первый экран бота — одна кнопка «Подключить VPN».
Бесплатные дни включаются сами при первом «Подключить» в мини-аппе,
отдельной кнопки под них нет. Мини-аппа не спрашивает, какое приложение,
а сразу пробует INCY; выбор «Установить INCY / У меня Happ / Другое»
появляется только если за 8 секунд ничего не открылось. Новичок доходит до
работающего VPN за три нажатия.

**Мини-аппа говорит с ботом.** Открытая из постоянной клавиатуры
(`?src=kb`), она шлёт `sendData`: «готово — вернуться в бот» → бот
отмечает подключение и показывает экран «ГОТОВО»; «не получается — позвать
помощь» → бот сам открывает экран помощи. Не человек ищет помощь — она
находит его.

**Карточка закреплена.** `pinChatMessage` на `/start` — кабинет всегда
наверху чата, под напоминаниями не тонет.

**Команды меню:** `/start` кабинет, `/connect`, `/help`, `/pay`.

**`/stats` — цифры для вас.** Только для id из `ADMINS` в `bot.py`: сколько
в боте, сколько на бесплатных днях, оплатили, истекло; сколько подключили
устройство и какая это доля; у скольких кончается в сутки; сколько включили
дни за последние 24 часа; сколько пришли по приглашению. Считается из `store.py`
на лету. Не-админу `/stats` показывает кабинет, как любая незнакомая команда.

**Приглашения замкнуты.** `/start ref<id>` записывает, кто привёл (один раз,
не себя, только если пригласивший есть в боте). Пришедший видит на первом
экране «Вас пригласил Хан — первые 3 дня бесплатно». Пригласивший на экране
«Пригласить друга» видит «Пришли по ней: N · подключили VPN: M», кнопка
Начисление вознаграждения — вместе с оплатой.

**«Проверить VPN».** На экране ГОТОВО кнопка открывает сайт, который без VPN
не работает (`CHECK_URL`, сейчас Instagram — заменить на свой): открылся —
работает, нет — «Помощь». Наивному пользователю это ценнее любого статуса:
он видит результат своими глазами. То же в мини-аппе на экране «Подключено».

**Карточка на новых баннерах:** «ПОМОЩЬ / Напишите сюда» — экран помощи;
«ГОТОВО / VPN подключён» — после сигнала из мини-аппы и на `activated`,
если устройство уже настроено.

## Как устроен поток

**Одна живая карточка.** Бот перерисовывает одно сообщение, а не шлёт
новые. При `/start` предыдущая карточка удаляется; если «Кабинет» нажали
в напоминании, оно убирается, а карточка обновляется.

**Навигация.** На каждом вложенном экране одна кнопка «◀️ Назад» — на шаг
выше. «🏠 Кабинет» живёт на постоянной клавиатуре внизу и в карточках
не дублируется.

**Кнопки влезают в телефон.** Telegram режет длинные подписи: на экране
390 px полноширинная кнопка держит ~24 символа, кнопка в паре — ~12.
Проверка длины есть в прогоне перед запуском. Каждая подпись называет,
куда попадёшь: «📘 Настройка», «📣 Новости», «🆘 Помощь».

**Инструкции живут в мини-аппе.** Бот выбирает устройство и открывает
её на нём: `?platform=ios`. Платформы подписаны марками — «Samsung,
Xiaomi, Huawei», «MacBook или iMac», — человек знает марку, а не систему.

**Бот отвечает на любой текст.** Любое сообщение — обращение в поддержку
(см. выше), а по приметам сразу показывает раздел: «продлить» → тарифы,
«настроить» → инструкции.

**Бот пишет первым.** Фоновый обход раз в 15 минут (`notifier()`): через
час после активации без подключения; когда осталось меньше суток; когда
закончилось. Слова разные для бесплатных дней и оплаты. Время только
относительное — «через 9 часов», не «в 21:40 UTC». Ночью по Москве
(23–9) молчит. Каждое напоминание — раз за период: ключ привязан
к дате окончания.

## Тексты

Аудитория — люди, не знающие слов «ключ», «соединение», «Android».
Правила, по которым написан `screens.py`:

- говорим, что человек увидит и что нажать, а не как это устроено;
- одно действие на экран — крупно; остальное ниже и короче;
- кнопка называет результат: «Подключить VPN», а не «Далее»;
- ничего, что выглядит как кнопка и не нажимается;
- ни одного тупика: любой экран ведёт дальше или к живому человеку;
- цена после бесплатных дней названа заранее — люди боятся скрытых списаний.

## Что заглушено

Всё в начале `screens.py`, помечено `TODO`: цены в `TARIFFS`,
`REFERRAL_REWARD`, `CHANNEL_URL`, `SUPPORT_URL`, `TERMS_URL`.
Оплата — демо-кнопка. Статистика приглашений — ждёт базы.
